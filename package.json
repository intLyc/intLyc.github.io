{
  "scripts": {
    "lint:prettier": "prettier . --check",
    "test:data": "python3 -m unittest discover -s test -v",
    "test": "npm run test:data && npm run lint:prettier"
  },
  "devDependencies": {
    "@shopify/prettier-plugin-liquid": "^1.10.0",
    "prettier": "^3.9.6",
    "purgecss": "8.0.0"
  }
}
